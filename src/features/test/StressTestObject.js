export class StressTestObject {
  constructor(score, testQuestions, userEmail, durationTime, date) {
    this.score = score;
    this.testQuestions = testQuestions;
    this.userEmail = userEmail;
    this.durationTime = durationTime;
    this.date = date;
  }
}
